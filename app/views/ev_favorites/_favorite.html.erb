<%= form_for(current_user.ev_favorites.build(event_id: event_obj.id), remote: true) do |f| %>
	<%= hidden_field_tag :event_id, event_obj.id %>
	<%= f.submit "Add to My Favorite", class: "btn btn-small btn btn-info" %>
<% end %>
