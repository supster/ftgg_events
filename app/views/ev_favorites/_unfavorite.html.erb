<%= form_for(current_user.ev_favorites.find_by_event_id(event_obj.id), html: { method: :delete }, remote: true) do |f| %>
	<%= hidden_field_tag :event_id, event_obj.id %>
	<%= f.submit "Remove from My Favorite", class: "btn btn-small" %>
<% end %>